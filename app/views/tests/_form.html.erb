<%= javascript_include_tag "thresholdtest", "data-turbo-track": "reload" %>
<%= form_with(model: test) do |form| %>
  <% if test.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(test.errors.count, "error") %> prohibited this test from being saved:</h2>

      <ul>
        <% test.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <strong><%= form.label "Name:" %></strong>
    <%= form.text_field :name %>
  </p>

  <p>
    <% if @test.course_module_id.nil? %>
      <%= form.hidden_field :course_module_id, value: @module_id %>
    <% else %>
      <%= form.hidden_field :course_module_id, value: @test.course_module_id %>
    <% end %>
  </p>

  <p id="max_score_div">
   <strong><%= form.label "Max score:" %></strong>
    <%= form.number_field :max_score %>
  </p>

  <p id="earned_score_div">
    <strong><%= form.label "Earned score:" %></strong>
    <%= form.text_field :earned_score %>
  </p>

  <p id="weighting_div">
    <strong><%= form.label "Weight:" %></strong>
    <%= form.text_field :weight %>
  </p>

  <p>
    <strong><%= form.label :is_threshold? %></strong>
    <%= form.check_box :is_threshold %>
  </p>

  <p id="is_pass_div" style="display: none">
    <strong><%= form.label :pass? %></strong>
    <%= form.check_box :pass %>
  </p>

  <p>
    <%= form.submit class: "utility-button" %>
  </p>
<% end %>
